<Window x:Class="RejestrFaktur.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RejestrFaktur.Views"
        mc:Ignorable="d"
        Title="Rejestr Faktur" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Margin="50" Cursor="Hand" Command="{Binding BrowseInvoicesButton}">
            HISTORIA FAKTUR
        </Button>
        <Button Grid.Column="1" Margin="50" Cursor="Hand" Command="{Binding CreateInvoiceButton}">
            NOWA FAKTURA
        </Button>
    </Grid>
</Window>
